/**
 * @description The function takes a value as input and returns `true` if the value
 * is not null or undefined and `false` otherwise.
 * 
 * @param { any } val - Here's your function again for convenience:
 * 
 * (val) => !!val
 * 
 * The single-word description of what `val` does would be "provides the input to the
 * function".
 * 
 * @returns { boolean } The function takes a single argument "val" and returns its
 * boolean negation (i.e., whether the input is falsy or not). In other words it
 * returns "true" if the input is truthy (i.e. neither null nor empty string) and
 * "false" otherwise.
 */
const test = (val) => !!val;
